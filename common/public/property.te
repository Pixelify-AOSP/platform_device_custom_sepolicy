# NFC
system_vendor_config_prop(vendor_persist_nfc_prop)

# USB
system_vendor_config_prop(usb_uvc_config_prop)

# xtra-daemon control
system_restricted_prop(xtra_control_prop)

# Keystore compatibility
system_public_prop(keystore_compat_prop)

